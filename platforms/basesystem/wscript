top = '../..'

def build(bld):
    use   = 'ahbctrl ahbmem apbctrl sr_signal usi common pytools sr_registry sr_register sr_report'
    use  += ' ahbdisplay ahbcamera ahbgrayframer ahbframetrigger ahbshuffler ahbdemosoftware AMBA TLM GREENSOCS SYSTEMC BOOST'
      
    bld(
        target       = 'basesystem.platform',
        features     = 'cxx cprogram pyembed',
        source       = 'sc_main.cpp',
        includes     = '.',
        use          = use,
        #ut_param     = ['singlecore.lt.prom', 'irqmp.sparc'],
    )

